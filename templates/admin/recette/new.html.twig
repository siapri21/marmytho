{% extends 'base.html.twig' %}

{% block title %}New Recipe{% endblock %}

{% block body %}
    <h1>Create new Recipe</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        
        <button class="btn">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_recette_index') }}">back to list</a>
{% endblock %}
